# 项目密钥：复制为同目录 .env 并填入真实值（.env 已被 gitignore，不入库）
# 由 compose.yml 的 env_file: ./api/.env 直接注入容器进程
# 变量名带项目前缀避免歧义；生成随机密钥：openssl rand -hex 32
DEMO_JWT_SECRET=
# 初始管理员密码（仅用户表为空时播种生效，建好后可清空）
DEMO_ADMIN_PASSWORD=
