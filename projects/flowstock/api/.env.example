# FlowStock 密钥：复制为同目录 .env 并填入真实值（.env 已被 gitignore）
#
# 2026-09-07：数据库已切换为 SQLite-only（应用内置纯 Go 驱动，compose 固定 DB_FILE=/app/data/flowstock.db）。
# 以下 DATABASE_URL / DB_DRIVER 已失效（应用不再读取），仅 JWT_SECRET / PORT 仍生效。
JWT_SECRET=

# 固定值（一般不用改）
PORT=8081
